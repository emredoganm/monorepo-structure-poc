{
  "npmClient": "yarn",
  "packages": ["packages/**"],
  "useWorkspaces": true,
  "version": "independent",
  "ignoreChanges": ["**/*.test.*", "**/*.stories.*", "**/*.md"],
  "command": {
    "version": {
      "conventionalCommits": true,
      "message": "chore(release): publish"
    }
  }
}
